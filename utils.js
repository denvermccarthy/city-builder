export function createStatsString(archCount, climateCount, knownCount) {
    return `Wow! You've changed the architecture ${archCount} times! You've changed the climate ${climateCount} times, and you changed what your city is known for ${knownCount} times. Great work!`;
}